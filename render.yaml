services:
  - type: web
    name: final-agent-ui
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main_flask.py
    build:
      environment:
        pythonVersion: 3.11.8

